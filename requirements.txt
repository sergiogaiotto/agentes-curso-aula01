# --- Núcleo de agentes ---
langgraph>=1.0,<2.0
langchain>=1.0,<2.0
langchain-openai>=1.0,<2.0

# --- API web ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Observabilidade ---
langfuse>=3.0

# --- RAG e memória (Aula 3) ---
langchain-postgres>=0.0.17
langgraph-checkpoint-postgres>=2.0
langchain-text-splitters>=0.3

# --- Utilidades ---
pydantic>=2.9
python-dotenv>=1.0

# --- Banco / drivers ---
psycopg[binary]>=3.2
pgvector>=0.3

# --- Cliente HTTP para ferramentas de integração (Aula 4) ---
httpx>=0.27

# --- Integração MCP / Model Context Protocol (Aula 9) ---
# 'mcp' fornece o FastMCP usado em app/mcp_server.py;
# 'langchain-mcp-adapters' expõe as tools MCP como tools LangChain
# (MultiServerMCPClient em app/mcp_client.py). A versão >=0.3 exige
# langchain-core >=1.0, alinhada com o langchain>=1.0 deste projeto.
mcp>=1.9.2
langchain-mcp-adapters>=0.3
